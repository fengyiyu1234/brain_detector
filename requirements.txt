# ============================================================
# brain_detector — Python dependency list
# Python 3.9 recommended
# ============================================================

# ── Core scientific stack ────────────────────────────────────
numpy>=1.24,<2.1
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3          # GaussianMixture in stitcher.py
tqdm>=4.65

# ── Image processing ─────────────────────────────────────────
# Use headless variant on servers with no display; use the
# regular variant (opencv-python) if you need GUI windows.
opencv-python-headless>=4.8
Pillow>=10.0               # used by napari / image I/O

# ── Deep learning — PyTorch ──────────────────────────────────
# GPU build (CUDA 11.8, Windows / Linux):
#   pip install torch==2.1.2+cu118 torchvision==0.16.2+cu118 \
#               --index-url https://download.pytorch.org/whl/cu118
#
# GPU build (CUDA 12.1):
#   pip install torch==2.1.2+cu121 torchvision==0.16.2+cu121 \
#               --index-url https://download.pytorch.org/whl/cu121
#
# CPU-only (macOS / no GPU):
#   pip install torch torchvision
#
# The line below is the CPU/default fallback; comment it out
# and use one of the GPU commands above on a CUDA machine.
torch>=2.1.0
torchvision>=0.16.0

# ── Detection models ─────────────────────────────────────────
ultralytics>=8.0           # YOLOv8/v9/v10 (YOLO class)
cellpose>=2.2              # Cellpose nucleus segmentation

# ── Visualization ────────────────────────────────────────────
napari>=0.4.19
PyQt5>=5.15                # napari GUI backend (swap for PyQt6 if preferred)
