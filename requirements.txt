# ============================================================
# brain_detector — Python dependency list
# Python 3.9 recommended
#
# Two environments:
#   1. LOCAL (macOS)  — visualization + code editing only
#   2. HPC   (Linux)  — full inference with GPU
# ============================================================


# ── [BOTH] Core scientific stack ─────────────────────────────
numpy>=1.24,<2.1
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3          # GaussianMixture in stitcher.py
tqdm>=4.65

# ── [BOTH] Image I/O ─────────────────────────────────────────
opencv-python-headless>=4.8
Pillow>=10.0

# ── [LOCAL only] Visualization ───────────────────────────────
# macOS: pip install napari PyQt5
napari>=0.4.19
PyQt5>=5.15

# ── [HPC only] PyTorch + CUDA ────────────────────────────────
# On HPC (Linux, CUDA 11.8):
#   pip install torch==2.1.2+cu118 torchvision==0.16.2+cu118 \
#               --index-url https://download.pytorch.org/whl/cu118
#
# On HPC (Linux, CUDA 12.1):
#   pip install torch==2.2.0+cu121 torchvision==0.19.0+cu121 \
#               --index-url https://download.pytorch.org/whl/cu121
#
# Check your HPC CUDA version first:  nvcc --version

# ── [HPC only] Detection models ──────────────────────────────
# pip install ultralytics>=8.0 cellpose>=2.2
